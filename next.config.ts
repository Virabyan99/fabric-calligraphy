const nextConfig = {
  typescript: {
    ignoreBuildErrors: true, // Disables TypeScript errors
  },
  eslint: {
    ignoreDuringBuilds: true, // Disables ESLint errors
  },
};

export default nextConfig;
